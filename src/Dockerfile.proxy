# NOTE: This dockerfile creates an nginx reverse proxy.
#       This container would be the endpoint ateway to all the other containers
#       and is used to handle certificates.
#       .
#       It is useful for logging internal network traffic.

FROM nginx:1.21.3-alpine
COPY proxy/default.conf /etc/nginx/conf.d/default.conf
COPY proxy/ssl /etc/nginx/ssl
COPY proxy/includes/ /etc/nginx/includes/
